---
"@baldin/plugin-puppeteer": minor
---

Extract browser automation into an independently publishable plugin with its
own Puppeteer, stealth, user-agent, and cursor dependencies; preserve proxy,
cookie, monitoring, performance, storage, and anti-bot capabilities; and make
nested configuration types reflect the runtime defaults.

---
"@baldin/core": patch
"@baldin/plugin-eventual-consistency": minor
---

Keep similarly named partition namespaces isolated during updates and add the standalone eventual-consistency plugin with record-scoped consolidation, coordinated workers, analytics, and lifecycle-safe resource helpers.

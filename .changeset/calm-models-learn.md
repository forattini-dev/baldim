---
"@baldin/plugin-ml": minor
---

Add the standalone ML plugin with regression, classification, time-series, and
custom neural-network models. Bundle the portable TensorFlow.js runtime in the
plugin package and preserve training, prediction, persistence, versioning,
automatic training, and resource namespace APIs.

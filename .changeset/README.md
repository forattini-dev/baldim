# Releases

Run `pnpm changeset` for a change to a package's public API or behavior.
Packages are versioned independently. `pnpm version-packages` applies pending changesets.
The bootstrap is not published. npm scope ownership and release readiness must be
established before publishing any package. CI does not publish to npm.

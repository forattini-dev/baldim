---
"@baldin/plugin-websocket": minor
---

Extract the dedicated Raffel WebSocket plugin with resource CRUD, subscriptions,
channels, authentication, connection tickets, rate limiting, compression,
recovery, health endpoints, and its original integration suites.

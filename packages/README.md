# Shared packages

This directory is reserved for independently publishable building blocks used by the core and multiple plugins or adapters. Code stays within its owning package until there is a proven shared contract.

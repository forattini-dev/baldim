# @baldim/cli

The Baldim command-line interface.

```sh
baldim --help
baldim configure
baldim resources list --connection memory://demo
```

The CLI includes all official storage adapters and can generate types, run migrations, seed data, inspect resources, and open an interactive shell.
